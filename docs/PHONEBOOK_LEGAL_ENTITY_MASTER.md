# /phonebook — master-справочник юридических лиц

Дата решения: 13.09.2026
Последнее production-обновление: 15.09.2026
Статус: **PRODUCTION / ACCEPTED, follow-up cleanup продолжается**
Jira: KAN-77
Исходная feature-ветка: `feature/v1.8-phonebook-legal-entities`

## Цель

Не создавать отдельную базу реквизитов и не дублировать юридические данные в разных модулях. Существующая PostgreSQL Evrasia AI Bot является единым master-data контуром `/phonebook`.

Существующая таблица `corporate_legal_entities` расширена и является source of truth по ЮЛ. Рестораны, корпоративные телефоны, лицевые счета и будущие документы должны ссылаться на ЮЛ через `legal_entity_id`.

## Главное правило source of truth

Если у записи есть `legal_entity_id`, отображаемые название ЮЛ, ИНН и генеральный директор читаются из `corporate_legal_entities`, а не из legacy-копий в таблицах телефонов/ресторанов.

Legacy-поля `legal_entity`, `inn`, `general_director` сохраняются только как fallback/backward compatibility для ещё не связанных записей и для безопасного переходного периода.

Следствие: изменение master-карточки ЮЛ должно автоматически отражаться во всех consumers `/phonebook` без ручного редактирования каждой связанной записи.

## UI

В `/phonebook` есть вкладка `Реквизиты` между `Рестораны и директора` и `МегаФон`.

Целевая последовательность:

`Рестораны и директора | Реквизиты | МегаФон | T2 | История`

Вкладка позволяет:

- искать и фильтровать ЮЛ;
- добавлять ЮЛ;
- редактировать ЮЛ;
- архивировать ЮЛ вместо физического удаления;
- видеть юридические реквизиты, адреса, генерального директора, банковские счета, операторские лицевые счета и связанные объекты.

### Удаление / архивирование ЮЛ

**Было:** удаление строки реквизитов запрашивало `PHONEBOOK_WEB_WRITE_TOKEN` через editor prompt, а backend DELETE использовал общий `requireEditor()`.

**Стало с 15.09.2026:** для удаления/архивирования ЮЛ используется только обычный confirm `Уверены, что хотите удалить?`; именно DELETE legal-entity не требует write-token. Создание и редактирование остаются под существующей защитой.

**Причина:** пользователь не заказывал отдельный ключ для удаления; целостность данных обеспечивает archive guard по активным связям.

Архивирование защищено guard: ЮЛ нельзя архивировать, пока на `legal_entity_id` ссылаются активные телефоны или рестораны. Сначала связи должны быть переназначены, закрыты или доказанно признаны stale legacy хвостом. История при этом сохраняется.

## Stale legacy phone rule

Если phone row присутствует только в `corporate_phone_directory`, отсутствует во всех доступных phone/source-like таблицах вне aggregate, не используется restaurant personal-phone fields и не имеет другого активного владельца с тем же номером, строку допускается деактивировать (`active=false`) как stale legacy хвост после backup + exact guards + транзакции + post-check.

Физический DELETE таких phone rows по умолчанию не используется.

На production discovery 15.09.2026 отдельные таблицы MegaFon/T2 в PostgreSQL `evrasia_ai_bot` не обнаружены; среди phone-like объектов найдены:

- `corporate_directory_restaurants`;
- `corporate_phone_audit`;
- `corporate_phone_directory`.

Поэтому документировать проверку следует как `NOT_FOUND_OUTSIDE_AGGREGATE` в доступных таблицах, а не как отдельный запрос к внешней системе оператора.

Подробный production follow-up: `docs/PHONEBOOK_PRODUCTION_FOLLOWUP_2026-09-15.md`.

## Идентичность ЮЛ

Название не является уникальным ключом.

Подтверждены два разных ЮЛ с одинаковым названием `Евразия-Триумф`:

- Новаторов 32 — ИНН `7817318001`, ОГРН `1097847304609`;
- Московский 222 — ИНН `7810591971`, ОГРН `1107847187524`.

Поэтому UI dropdown должен показывать как минимум `Название + ИНН`.

На этапе миграции существующих данных автоматическая связь по одному названию допускается только если название однозначно соответствует одному активному ЮЛ.

## Генеральные директора

Актуальный source of truth: файл `Списки дир-ов от 06.04.26.xlsx`, лист `Лист1`, колонка `ФИО директора (генерального)`.

Старые Word/XLS карточки реквизитов могут храниться как источник/история, но не должны автоматически переопределять более свежий источник.

## Адреса

Хранятся разные сущности:

1. `legal_address`, `actual_address`, `postal_address` ЮЛ — полные официальные значения без сокращения и смыслового исправления;
2. короткий адрес ресторана / display address — существующий компактный формат типа `Энгельса 21`, `Московский 222`, `Новаторов 32`.

`legal_address` и `actual_address` могут полностью различаться, вплоть до разных улиц; нельзя автоматически считать их одинаковыми.

Существующие короткие адреса ресторанов нельзя перезаписывать официальным длинным адресом ЮЛ.

Для новых ресторанов допустима автоматическая генерация короткого адреса из полного адреса, но результат должен быть редактируемым до сохранения.

## Модель данных

Используется одна БД и связанные таблицы:

- `corporate_legal_entities` — master ЮЛ;
- `corporate_legal_entity_bank_accounts` — один или несколько банковских счетов ЮЛ;
- `corporate_legal_entity_operator_accounts` — лицевые счета/договоры операторов;
- `corporate_phone_directory.legal_entity_id` — связь номера с master ЮЛ;
- `corporate_directory_restaurants.legal_entity_id` — связь ресторана с master ЮЛ.

На первом rollout legacy-поля `legal_entity`, `inn`, `account_number`, `general_director` сохранены для обратной совместимости. Их нельзя удалять до полного перевода всех consumers и production-проверки.

## Валидация импортируемых реквизитов

Сомнительные значения не исправляются автоматически.

Для конфликтующих/подозрительных значений сохраняются:

- исходное значение;
- источник;
- `verification_status` (`VERIFIED`, `NEEDS_REVIEW`, `UNVERIFIED`);
- при необходимости комментарий/notes.

Нельзя объединять ЮЛ по ОМС, ПФ, email, адресу или похожему имени.

Банковские реквизиты, ОГРН/КПП, ОКВЭД и регистрационные номера из старых карточек с конфликтами должны пройти review перед установкой статуса VERIFIED.

## Формы phonebook

### Корпоративный номер

Поле `Юридическое лицо` должно быть searchable dropdown.

После выбора:

- название и ИНН берутся из master ЮЛ;
- ИНН не вводится вручную;
- лицевой счёт выбирается из связанных операторских счетов ЮЛ;
- если счетов несколько — пользователь выбирает нужный.

### Ресторан

Поле `Юридическое лицо` должно быть searchable dropdown.

После выбора:

- генеральный директор берётся из master ЮЛ и должен заполняться в форме ресторана;
- короткий адрес ресторана сохраняется отдельно и не заменяется полным `actual_address` ЮЛ.

## Документы v1.8

Шаблоны документов не должны содержать захардкоженные реквизиты.

Цепочка данных:

`phonebook context -> legal_entity_id -> master ЮЛ -> реквизиты/счета -> DOCX/PDF`

Это обеспечивает изменение реквизитов в одном месте и использование актуального значения во всех будущих документах.

## Миграции и CI

`0022_phonebook_legal_entity_master.sql` является первым schema rollout этого решения и уже применён в production.

После добавления миграции обязательно одновременно:

- обновить `lib/db/drizzle/meta/_journal.json`;
- обновить hardcoded migration count в CI;
- добавить feature-specific schema smoke test.

DO NOT REPEAT: не оставлять CI с ожиданием старого количества миграций, как это произошло при rollout `0021`.

## Production rollout state

Подтверждено:

- migration count = `23`;
- master dedupe `91 -> 68`;
- duplicate INN groups `23 -> 0`;
- все 62 restaurant rows были связаны с master на rollout;
- verified canonical core import выполнен;
- последующий address/core requisites backfill выполнен отдельными контролируемыми этапами;
- 18 stale phone rows четырёх старых ЮЛ деактивированы 15.09.2026;
- 7 stale legacy rows `ООО "Евразия-Большевиков"` также успешно деактивированы 15.09.2026;
- production image после PR #50: `sha256:f86c59983ef1857cf26b9763cd019d809ff821a8e2b01904c2b3b408f8f0eb5c`.

## Открытая точка продолжения

Ресторан №28 `Большевиков 18` на момент последней проверки остаётся `active=true` и связан с `ООО "Евразия-Манхеттен"` (`legal_entity_id=le-b534bcce3b02ace0d23bfac2`). Пользователь сообщил, что ресторан закрыт в мае 2026 года.

У `ООО "Евразия-Манхеттен"` обнаружено 8 active phone rows. Эти 8 строк **не относятся автоматически** к семи legacy rows `ООО "Евразия-Большевиков"` и пока не деактивировались.

Следующий шаг: отдельно определить, какие из этих 8 номеров действительно относятся к закрытому ресторану №28 и могут быть закрыты, не затрагивая другие возможные связи `Евразия-Манхеттен`.
