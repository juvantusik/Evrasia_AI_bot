# SamZaberu GitHub snapshot v1.4

Clean source snapshot prepared for the private GitHub repository.

Excluded from this archive: Replit runtime metadata, `.git`, build outputs, caches, screenshots, uploaded assets, local `.env` files and real secret values.

Required environment variables are documented in `.env.example`; provide real values only through the target hosting platform's secret/environment settings.
