# Git metadata and local tooling
.git
.github
.vscode
.idea

# Secrets: never copy runtime configuration into an image
.env
.env.*
!.env.example
*.pem
*.key

# Dependencies and build output are rebuilt inside the image
node_modules
**/node_modules
dist
**/dist
coverage
**/coverage

# Server-only and temporary artifacts
prebuilt
*.zip
*.dump
*.log
